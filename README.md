# Leather

面向信息学竞赛生的本地优先代码辅助站。它把代码模板沉淀、随机数据对拍和训练任务规划放进同一个轻量工作台，可直接使用 GitHub Pages 免费托管。

## 功能

- **加密模板库**：用关键字定位代码库、用密码解锁；支持分区、标签页、语言与标签筛选。
- **版本快照与 Diff**：每次主动保存都会记录完整快照，可逐行查看新增、删除与未变内容，也能把旧版本载入编辑器。
- **代码对拍**：JavaScript 代码可在隔离的 Web Worker 中一键运行，自动保留第一组差异、输入、双方输出和随机种子。
- **C++14 本地对拍**：网页可把三段 C++ 程序打包进 PowerShell 对拍脚本，在装有 `g++` 的 Windows 电脑上编译运行。
- **随机数据模板**：内置随机数组、随机整数和随机连通图生成器，JavaScript 与 C++14 各有一套。
- **分层任务导图**：上层优先级高于下层；每层可放多个任务，支持跨层拖动、层级排序和完成率统计。

## 项目的合理优势

Leather 的优势不是功能数量，而是部署和数据边界清晰：项目只有原生 HTML、CSS 和 JavaScript，没有服务端、数据库和构建依赖；GitHub Pages 即可长期托管。模板在写入 `localStorage` 前使用 PBKDF2（180,000 次 SHA-256）派生的 AES-256-GCM 密钥加密，源码不会被上传。对拍与任务规划围绕竞赛训练中的高频流程设计，操作路径短，也便于以后继续扩展。

相应地，本地优先也有明确代价：浏览器数据不会自动跨设备同步，清理站点数据会丢失内容，因此应定期导出模板库密文备份；密码没有找回机制。

## 本地运行

直接打开 `index.html` 可以浏览多数页面。模板加密依赖 Web Crypto，建议通过本地 HTTP 服务运行：

```bash
python -m http.server 8080
```

然后访问 `http://localhost:8080`。

## 部署到 GitHub Pages

1. 将这些文件提交并推送到 GitHub 的 `leather` 仓库默认分支。
2. 打开仓库 **Settings → Pages**。
3. 在 **Build and deployment** 中选择 **Deploy from a branch**。
4. 选择默认分支和 `/ (root)`，点击 **Save**。
5. 等待 GitHub 给出站点地址，通常形如 `https://<用户名>.github.io/leather/`。

站点使用 Hash 路由，不需要额外的 404 重定向或 Actions 构建流程。

## 对拍约定

在线 JavaScript 模式：

- 正解和暴力各自定义 `solve(input)`，返回字符串或可转为字符串的值。
- 生成器定义 `generate(seed)`，返回一组完整输入。
- 生成器可调用内置的 `rnd(l, r)`，它返回闭区间随机整数。
- 比较时仅忽略输出末尾空白，内部空格与换行仍会严格比较。
- 单次任务最长运行 20 秒，失控代码会随 Worker 一同终止。

C++14 本地模式：

- 三个编辑器中都放完整且能独立编译的程序。
- 生成器从 `argv[1]` 读取随机种子并向标准输出写入数据。
- 下载脚本后需在 Windows PowerShell 中运行；本机应安装 `g++` 并加入 `PATH`。

## 安全说明

- 密码不会写入本地存储，只在当前页面会话的内存中用于派生密钥。
- 关键字经 SHA-256 后用于生成本地存储键；密文备份中会包含代码库名称和关键字，代码内容仍为密文。
- 本项目没有云端帐号与恢复服务。忘记密码时，任何人都无法解密对应模板。
- 在线对拍代码在 Worker 中运行，但仍不应粘贴来源不明或包含隐私的数据。

## 技术结构

```text
index.html   页面语义与四个主视图
styles.css   响应式布局和视觉样式
app.js       路由、加密存储、Diff、对拍与任务导图逻辑
```

## 浏览器支持

建议使用较新的 Chrome、Edge 或 Firefox。模板库要求浏览器支持 Web Crypto、AES-GCM 和 PBKDF2。

## License

如需公开协作，建议在仓库中补充你希望采用的开源许可证；当前仓库尚未指定许可证。
